var str = "Good Morning IT champ";
var lowerCase = str.toLowerCase();
var lowerCaseVowels = lowerCase.includes(char)
for (let index = 0; index < str.length; index++) {
    var char = str.charAt(index);
    if (char) {
        console.log(char);
    }
}
